#include<stdio.h>
int main()
{
    int i,n,m,count=0,sum=0;
    scanf("%d%d",&n,&m);
    for(i=n;i<m;i++)
    {
        if(i%9==0)
        {
            printf("%d.",i);
            count++;
            sum=sum+i;
        }
    }
    printf("\nno.of elements: %d",count);
    printf("\nsum is: %d",sum);
    return 0;
}

