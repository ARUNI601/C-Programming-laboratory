#include<stdio.h>
int main()
{
    int n,i,t,s;
    printf("enter the table:");
    scanf("%d",&t);
    printf("enter upto:");
    scanf("%d",&n);
    for (i=1;i<=n;i++)
    {
        s=0;
        s=t*i;
        printf("%d*%d=%d\n",t,i,s);
    }
    return 0;
}
